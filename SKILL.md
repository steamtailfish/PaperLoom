---
name: paper-loom
description: 将论文 PDF 制作为紧凑、图表优先、可编辑的科研组会 PowerPoint，或按反馈局部修改现有论文 PPT。支持客户端安装和网页端上传整个 skill ZIP；使用仿宋 GB2312、Times New Roman、原生 Office 公式与直接提取的论文图。
---

# PaperLoom · 论文织图

交付可用于科研组会的 `.pptx`。阅读论文、组织证据、设计页面并实际生成文件；不要只给大纲。以下为本 skill 的默认偏好，用户在当前任务中的明确要求优先。

## 先选择使用方式

- **客户端已安装**：从本文件所在目录解析 `scripts/`、`fonts/`、`references/`。先读 [客户端指南](references/client-mode.md)。
- **网页端上传 ZIP**：解压用户提供的整个包，找到同一根目录下的 `SKILL.md` 和 `fonts/`；先读 [网页入口](WEB_START.md)。上传不等于永久安装。
- 使用当前环境已有、受支持的演示文稿工具；若无专用工具，使用公开的 PptxGenJS 和本包脚本。示例代码没有依赖特定平台的内部 API。
- 检查文件是否真实存在、脚本依赖和字体，不使用历史会话的绝对路径。缺失的必要输入要明确指出；能独立完成的分析继续进行。没有文件生成能力时，不伪称已导出 PPTX。

## 固定的交付质量

1. **PowerPoint 原生文件**：文字、块、连接线、表格和数据图表尽量可编辑。不能用整页 PNG、PDF 或 HTML 冒充 PPTX。
2. **图表优先、提高有效密度**：图能说明的内容用图；精确数值比较保留表格。合并重复背景和空泛过渡，不能靠持续缩小字号塞内容。
3. **字体**：中文用 `FangSong_GB2312`（仿宋 GB2312），英文和数字用 `Times New Roman`。按文本 run 分配，不能只设置主题或把其他字体改名冒充。核验 [字体说明](fonts/README.md) 和 `fonts/manifest.json`。
4. **论文图禁止截图提取**：优先解出 PDF 内嵌 Image/Form 原始资源，见 [提图指南](references/figure-extraction.md)。页面渲染只用于阅读和质检，不能裁下来当论文图片。拆分的子图、额外标注和重绘必须对照原文确认完整性。
5. **公式少而必要，且原生可编辑**：只保留解释关键机制所需的公式；保留的 LaTeX 必须转换成 `a14:m / m:oMath`，对应 PowerPoint 插入公式的对象类型。禁止将 LaTeX 文本、SVG 或图片当作已转换公式。见 [公式与兼容性](references/powerpoint-compatibility.md)。
6. **证据在备注，页面不放来源小字**：论文章节、图表号、公式号、完整实验配置、标准差和分析边界写入演讲者备注及制作记录。底部仅保留轻量装饰和页码。直接理解数据必需的单位、图例、误差含义仍须在图表附近可见。
7. **不要把验证范围说大**：ZIP/XML 检查、渲染器导入、WPS 打开均不等于 Microsoft PowerPoint 桌面版验证。只有实际打开检查后才能作该声明。

## 制作流程

### 1. 建立证据和素材索引

通读正文、图注、表格和相关附录；记录任务、假设、技术模块、数据集、指标、实验设置、贡献和局限。建立 [evidence.template.json](examples/evidence.template.json) 所示索引。每项结论绑定页码或图表号，区分作者结论、复算结果与汇报者分析。不同表的划分、训练设置或统计口径不一致时不要直接相减。

提取图像后逐一检查；一个 XObject 可能只是一个面板、图例或 mask，不等于整张 Figure。方法图若需中文重绘，依据论文逻辑画可编辑结构，并把重绘依据记在备注。

### 2. 先分配页面

默认 **8–10 页，不额外加独立封面或目录**。首个内容页同时承载论文标识：

| 顺序 | 默认页数 | 这一部分要回答的问题 |
|---|---:|---|
| 现有问题 | 1 | 已有方法为什么仍然做不好？任务边界是什么？ |
| 文献综述 | 1 | 有哪些路线、各自提供什么能力、本文从哪里切入？ |
| 技术部分 | 3–5 | 核心机制如何实现，模块怎样互相作用？ |
| 实验 | 2 | 整体效果如何，哪些机制有效，有什么失败或代价？ |
| 总结 | 1 | 可借鉴的贡献、证据边界和有依据的改进方向是什么？ |

技术页数随论文工作量调整，不能把所有论文强行装进三个模块。每页先确定一个清楚的问题和主要视觉，再写短句。参考 [页面设计](references/design.md) 和 [GeoNav 制作复盘](examples/geonav/workflow.md)。

### 3. 用合适的视觉组织内容

- **综述**：优先 4–6 个研究路线块、两行三列或主题分组；每块含代表方法、能力、一个关键缺口或比较边界。不要默认使用密集大表格；无严格演化关系时不画时间推进箭头。
- **技术**：整体流程、模块分解、输入输出、空间/结构关系、跨步骤案例结合。公式嵌入相应机制，不独立堆成公式墙。
- **实验**：主结果页集中呈现关键对照和准确数值；第二页组织消融、泛化、效率、参数敏感性或失败分析。按论文实际证据选择，缺少的实验不要补造。
- **总结**：贡献对应证据，局限对应影响，建议明确为后续假设，不能伪装成论文已证明的结果。

### 4. 生成与验证

优先使用已有演示文稿工具；公开备用路线为：PptxGenJS 生成原生内容 → `inject_equations.py` 转换公式 → 按嵌入许可使用 `embed_fonts.py` → `validate_pptx.py` 检查 → 渲染逐页审阅。具体命令见 [README](README.md) 与示例。

注意中文字体 XML 的 `charset` 必须是有符号字节：GB2312 的 `0x86` 写作 `-122`，不能写 `134`；EOT 二进制的无符号字节仍为 `134`。不要为修一个字段而删除所有字体或将公式图片化。

检查完整 Figure、图表数值与单位、关键比较边界、文字溢出、字体、公式对象、来源备注和内部关系。发现版式问题先重组内容和尺寸，不随意更换用户指定字体。完整验证边界见兼容性指南。

### 5. 按反馈局部迭代

以用户最后认可的文件为基线。只改本次指定的页或元素，尽量保持其他页、字体、公式、媒体、备注和已修复的兼容性设置不变。用包内差异和前后渲染对照确认。小修优先定点替换原生对象，避免全量重导出造成丢失字体或公式。

交付清晰命名的最新版 PPTX。简短说明本次结果；不把制作日志、校验报告或截图当成用户要放映的内容。
