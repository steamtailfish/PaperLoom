# 网页端：上传两个文件，说一句话

上传 **PaperLoom 完整 ZIP** 和 **论文 PDF**，然后说：

> 请你按照压缩包内skill的要求制作论文的ppt

这就是全部操作。不需要阅读 README、复制详细提示词、填写配置或额外上传 AeroDuo 参考稿。这里的句子只是示例，同义请求也适用。

包内已附 [AeroDuo 六页参考 PDF](examples/aeroduo-reference/AeroDuo_组会汇报_合并精简6页版.pdf)，网页端与客户端均由 SKILL.md 引导实际查看，用户无需额外操作。

## 执行要求已经在包内

[SKILL.md](SKILL.md) 是供模型读取的完整入口。它负责定位论文、采用默认设置、读取设计规则和质量关卡，连续完成证据整理、逐页设计、PPTX 生成、渲染检查与修正，最后返回可下载文件。默认中文科研组会、16:9、完整讲解；每页通过 A/B/C 多分区展开相关要点，内容放不下就增加正文页，不设固定页数上限。核心机制与实验保持可见，补充讲解与来源放备注。

模型若先读到本文件，应立即读取 `SKILL.md` 并继续执行用户的制作请求，不要求用户重新发送提示词。用户提出页数、语言、模板或修改范围时，以用户要求为准。

## 仅在环境受限时

网页会话需要能解压、读取 PDF、执行代码和返回文件；上传 ZIP 本身不会增加这些能力，也不等于永久安装。执行者先尝试已有工具和公开依赖，确实无法继续时才说明具体缺项。不能以结构检查代替逐页看图，也不能声称完成未执行的渲染或 PowerPoint 打开检查。

日常上传使用包含字体的 `full` 包；`github` 变体是供开发者使用的源码包，不含独立字体文件。
