# 网页端：上传整个 ZIP + 论文

本文件是网页对话的入口。请先解压同次上传的 `paper-loom-*-full.zip`，读取根目录 `SKILL.md`，再按需读取它链接的指南。不要只读取 README 后凭印象制作。

## 可直接复制的提示词

> 请使用我上传的 PaperLoom 完整压缩包，为同时上传的论文制作科研组会 PowerPoint。先解压并读取 SKILL.md，使用 fonts/ 中的仿宋 GB2312 和 Times New Roman。默认结构为：问题 1 页、文献综述 1 页、技术 3–5 页、实验 2 页、总结 1 页。提高信息密度，文献综述用分块而不是大表格。论文图片直接提取原始资源，禁止截图；必要公式转为 PowerPoint 原生可编辑公式。来源对照写入备注，不放页脚小字。实际生成、检查并交付 .pptx，依据工作量决定技术页数，不要只给大纲。

## 在当前会话中执行

1. 确认论文和 ZIP 都在当前工作区；文件路径失效时先检查本次附件位置。使用 ZIP 根目录的相对结构，不继承此前会话路径。
2. 保持 `SKILL.md`、`references/`、`scripts/`、`fonts/` 和 `examples/` 的完整相对关系。不要把字体删除后仍声称使用了指定字体。
3. 运行 `python scripts/doctor.py` 查看依赖。网页环境必须有文件读取、解压、代码执行和文件导出能力；本包不能给纯文本聊天环境增加这些能力。
4. 有专用 PowerPoint 工具时使用该工具；否则使用公开 PptxGenJS。依赖不足时先使用当前环境已有能力，允许安装时再安装公开依赖。不能安装且阻断生成时明确告知缺项，不退化成伪装的 PPTX。
5. 阅读论文、整理证据、提取图片、制作并检查成品。正文事实始终以当前论文为准，不能复制示例的数字或模块。
6. 返回下载文件；ZIP 作为本次会话工作流资源，并不代表已经永久安装 skill。

仓库和完整包已包含 `fonts/README.md` 列出的字体，可将完整项目目录压缩后上传，或直接上传完整包。若使用 `scripts/package_skill.py --variant github` 生成的源码归档，则需先补齐字体，因为该打包选项不包含独立字体文件。
